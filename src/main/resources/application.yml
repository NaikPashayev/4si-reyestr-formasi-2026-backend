server:
  port: ${PORT:8080}
app:
  admin:
    password: ${ADMIN_PASSWORD:4sim4sim}
    token-secret: ${ADMIN_TOKEN_SECRET:;qn4oElX|ew]|MO)eg3Z0%'qM{XpIar~}
    token-ttl-hours: ${ADMIN_TOKEN_TTL_HOURS:12}
  cors:
    allowed-origins: ${ALLOWED_ORIGINS:http://localhost:5173,http://localhost:8080}
  storage:
    local-upload-dir: ${UPLOAD_DIR:/data/uploads}
    r2:
      endpoint: ${R2_ENDPOINT:}
      bucket: ${R2_BUCKET:}
      access-key-id: ${R2_ACCESS_KEY_ID:}
      secret-access-key: ${R2_SECRET_ACCESS_KEY:}
      region: ${R2_REGION:auto}
spring:
  datasource:
    url: ${DATABASE_URL:jdbc:h2:file:/data/innovate-registry;AUTO_SERVER=TRUE}
    driver-class-name: org.h2.Driver
    username: ${DATABASE_USERNAME:sa}
    password: ${DATABASE_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  h2:
    console:
      enabled: false
